# Shared paper-trading profile. Copy to profiles/paper.env and keep this file
# focused on global and per-strategy risk overrides. Strategy logic defaults live
# in config.py and strategy env_specs.
#
# Tickers: omit SYMBOLS so `data/<strategy>_plan.json` supplies strategy-local watch lists.
# Set SYMBOLS only in this file or in `.env` for global symbols shared by every active strategy.
# Strategies: set STRATEGIES here/`.env`, pass `-s`, or let the runner prompt.
# Do not `export` in the shell - `scripts/run_paper.sh` clears inherited SYMBOLS and STRATEGIES before loading files.

###############################################################################
# Runtime
###############################################################################

EXECUTION_MODE=alpaca_paper

###############################################################################
# Global Targets And Risk
###############################################################################

MAX_HOLD_SECONDS=420

STARTING_CASH=100000
MAX_OPEN_POSITIONS=64
TRADE_COOLDOWN_SECONDS=120

DAILY_MAX_LOSS=500
DAILY_MAX_LOSS_PCT=0.04
# Leave CONSECUTIVE_LOSS_*_COUNT unset to auto-scale from the active symbol count.

###############################################################################
# Per-Strategy Risk Overrides
###############################################################################

# Leave unset or 0 to use the global MAX_POSITION_VALUE, MAX_OPEN_POSITIONS,
# MAX_HOLD_SECONDS, and TRADE_COOLDOWN_SECONDS.
# STOCH_MACD_MAX_OPEN_POSITIONS=10
# STOCH_MACD_MAX_POSITION_VALUE=5000
# STOCH_MACD_MAX_HOLD_SECONDS=420
# MACD_MAX_OPEN_POSITIONS=10
# MACD_MAX_POSITION_VALUE=5000
# MACD_MAX_HOLD_SECONDS=420
# MACD_TRADE_COOLDOWN_SECONDS=180
# STEADY_INTRADAY_MAX_OPEN_POSITIONS=10
# STEADY_INTRADAY_MAX_POSITION_VALUE=5000
# STEADY_INTRADAY_MAX_HOLD_SECONDS=1200
# BP_MAX_OPEN_POSITIONS=10
# BP_MAX_POSITION_VALUE=5000
BP_MAX_HOLD_SECONDS=1800
BP_MAX_HOLD_DEFER_SECONDS=900

###############################################################################
# liquidity_scalper (tape stream)
###############################################################################

# MARKET_REGIME_BYPASS_STRATEGIES=liquidity_scalper
# LIQUIDITY_SCALPER_MAX_SPREAD_BPS=12
# LIQUIDITY_SCALPER_MIN_NET_EDGE_BPS=3
# LIQUIDITY_SCALPER_MIN_TAPE_DOLLAR_VOLUME=100000
# LIQUIDITY_SCALPER_MIN_TRADE_DOLLAR_VOLUME=10000
# LIQUIDITY_SCALPER_MIN_BUY_SELL_RATIO=1.35
